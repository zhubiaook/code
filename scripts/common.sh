#!/bin/env bash

# the root path of the scripts
PROJECT_ROOT=$(dirname "${BASH_SOURCE[0]}")

# color
source ${PROJECT_ROOT}/lib/color.sh
# log
source ${PROJECT_ROOT}/lib/log.sh
# init
source ${PROJECT_ROOT}/lib/init.sh
